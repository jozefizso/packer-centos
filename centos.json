{
  "variables": {
    "iso_url": "",
    "iso_checksum": "",
    "iso_checksum_type": "sha256",
    "redhat_release": "",
    "redhat_platform": "x86_64",
    "artifact_description": "",
    "artifact_version": "",
    "ssh_username": "vagrant",
    "ssh_password": "vagrant",
    "ssh_private_key_file": "~/.vagrant.d/insecure_private_key",
    "disk_size": "20480"
  },

  "builders": [
    {
      "type": "vmware-iso",
      "name": "centos-{{user `redhat_release`}}",

      "cpus": "2",
      "memory": "1024",

      "guest_os_type": "centos7-64",
      "http_directory": "http",
      "output_directory": "builds",

      "iso_url": "{{user `iso_url`}}",
      "iso_checksum": "{{user `iso_checksum`}}",
      "iso_checksum_type": "{{user `iso_checksum_type`}}",

      "ssh_username": "{{user `ssh_username`}}",
      "ssh_private_key_file": "{{user `ssh_private_key_file`}}",
      "ssh_wait_timeout": "15m",

      "boot_command": [
        "<tab> text ks=http://{{ .HTTPIP }}:{{ .HTTPPort }}/centos7.ks<enter><wait>"
      ],
      "shutdown_command": "sudo -S /sbin/halt -h -p",

      "disk_type_id": "0"
    }
  ],
  
  "post-processors": [
    {
      "output": "centos-{{user `redhat_release`}}-{{user `redhat_platform`}}-minimal.box",
      "compression_level": "6",
      "type": "vagrant"
    }
  ]
}
